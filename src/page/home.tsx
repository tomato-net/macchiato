import {Box, Typography} from "@mui/material";

export const Home: React.FC<{}> = () => {
    return(
        <Box>
            <Typography variant={`h1`}>
                Hello, World!
            </Typography>
        </Box>
    )
}